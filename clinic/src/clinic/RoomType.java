package clinic;

/**
 * This enumeration represents a room type. The room type enumeration
 * has values of an exam, a procedure, and a waiting room.
 */
public enum RoomType {
  EXAM, PROCEDURE, WAITING
}
